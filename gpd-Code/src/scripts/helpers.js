//Clear form if you have several different tabs. Import to all places with event listeners anc call during click listener.

export const clearPage = () => {
    document.querySelector(".criminalFlexContainer").innerHTML = ""
}